{{ form_start(form) }}
    {{ form_widget(form) }}
    
    <button class="btn btn-outline-primary mt-2">
        {{ button_label|default('Save') }}
    </button>
    <button class='btn btn-outline-primary mt-2'>
        <a class='company-button-anchor'  href="{{ path('company_index') }}">Cancel</a>
    </button>
{{ form_end(form) }}
