{% extends 'base.html.twig' %}

{% block title %}Company index{% endblock %}

{% block body %}
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid d-flex flex-row">
      <ul class="navbar-nav d-flex flex-row  w-50">
        <li class="nav-item p-1">
          <a class="nav-link" aria-current="page" href="{{ path('home') }}">CRUD</a>
        </li>
        <li class="nav-item p-1">
          <a class="nav-link" aria-current="page" href="{{ path('currency') }}">Exchange</a>
        </li>
      </ul>
      <ul class="navbar-nav d-flex flex-row  justify-content-end w-50">
        <li class="nav-item d-flex flex-row p-1">
          <div class="text-white nav-link">
          You are logged in as {{ app.user.username }},
          </div>
          <a class="nav-link text-center text-danger" href="{{ path('app_logout') }}"> Logout</a>
        </li>
      </ul>
    </div>
  </nav>
{% endblock %}
